# Переменная читается только конфигурацией Vite и не попадает в browser bundle.
MAP_BACKEND_ORIGIN=http://localhost:8080

# В browser попадает только имя provider, без API key. Альтернатива: stadia.
VITE_MAP_BASEMAP_PROVIDER=openfreemap
